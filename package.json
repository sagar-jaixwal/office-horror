{
  "name": "the-last-shift",
  "version": "1.0.0",
  "description": "The Last Shift - Horror Game",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "smoke": "node scripts/smoke.mjs",
    "models": "node scripts/prepare-models.mjs"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@gltf-transform/cli": "^4.4.2",
    "puppeteer-core": "^24.43.1",
    "vite": "^8.2.0"
  }
}
